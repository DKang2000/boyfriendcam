# Milestone 1A Manual QA Checklist

- Confirm the shot selector switches between all six templates without preview glitches.
- Confirm each shot type changes the ghost silhouette and framing guides immediately.
- Confirm `rule_of_thirds` and `instagram_story` show thirds guides instead of the crosshair shell.
- Confirm the ready shell changes to green when the subject roughly matches the selected template and falls back when they move away.
- Confirm portrait-upside-down rotation keeps the selected template overlay aligned with the preview.
- In a dev build, toggle debug mode on and off and verify the pose skeleton plus diagnostics panel appear and disappear cleanly.
- Confirm single-photo capture still works while any template is selected.
